from cf import CFContest, CFSpider
from ratings import get_cf_ratings
import leetcode
from atc import ATCContest, ATCSpider

# print(leetcode.get_daily_url())

# cfs = CFSpider()
# HOURS = 200
# contest_list = cfs.get_recent_contest(5, countdown_limit_hours=HOURS)
# print(contest_list)
# print(contest_list[0].countdown_as_str())

# atcs = ATCSpider()
# HOURS = 48
# atc_contest_list = atcs.get_recent_contest(5, countdown_limit_hours=HOURS)
# print(atc_contest_list)
